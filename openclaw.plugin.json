{
  "id": "lair",
  "name": "lair",
  "description": "workspace viewer — browse your agent's files in a configurable web UI",
  "version": "0.3.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "port": {
        "type": "number",
        "default": 3333,
        "description": "port for the lair web server"
      },
      "workspace": {
        "type": "string",
        "description": "path to serve (defaults to ~/.openclaw/workspace)"
      }
    }
  }
}
